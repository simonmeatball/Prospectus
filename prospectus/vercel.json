{
  "projects": {
    "client": "./client",
    "api": "./server"
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/server/api/$1" },
    { "source": "/(.*)", "destination": "/client/$1" }
  ]
}
